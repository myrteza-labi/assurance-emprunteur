import Maladies from "../maladies.json"; import Head from "next/head" ; export default function Hypercholesterolemie() { let test = Maladies.map((maladie) => { return maladie["data"][3]["contenu"];});return <div><Head> <meta name="description" content="Les emprunteurs présentant problème de sante hypercholestérolémie peuvent négocier les meilleures garanties et trouver ainsi une solution d\'assurance emprunteur avec risque aggravé de santé pris en charge"/> </Head><div className=" pageGenerees pagesMaladie" dangerouslySetInnerHTML={{ __html: test }}></div></div>;}