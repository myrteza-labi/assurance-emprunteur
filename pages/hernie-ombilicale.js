import Maladies from "../maladies.json"; import Head from "next/head" ; export default function Hernieombilicale() { let test = Maladies.map((maladie) => { return maladie["data"][23]["contenu"];});return <div><Head> <meta name="description" content="Réaliser un emprunt immobilier peut devenir compliqué en cas de maladie comme l\'hernie ombilicale pour obtenir une bonne couverture de credit"/> </Head><div className=" pageGenerees pagesMaladie" dangerouslySetInnerHTML={{ __html: test }}></div></div>;}