import Credits from "../credits.json"; import Head from "next/head" ; export default function Achatauto() { let test = Credits.map((credit) => { return credit["data"][5]["contenu"];});return <div><Head> <meta name="description" content="Vous souhaitez acheter une voiture mais ne pouvez pas la payer comptant ? Examiner les différentes possibilités qui vous sont proposées en terme d\'emprunt."/> </Head><div className=" pageGenerees pageCredit" dangerouslySetInnerHTML={{ __html: test }}></div></div>;}