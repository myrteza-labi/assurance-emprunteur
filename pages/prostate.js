import Maladies from "../maladies.json"; import Head from "next/head" ; export default function Prostate() { let test = Maladies.map((maladie) => { return maladie["data"][30]["contenu"];});return <div><Head> <meta name="description" content="Anticipez sur votre assurance emprunteur avec risque de santé prostate en faisant jouer la concurrence entre les assureurs !"/> </Head><div className=" pageGenerees pagesMaladie" dangerouslySetInnerHTML={{ __html: test }}></div></div>;}