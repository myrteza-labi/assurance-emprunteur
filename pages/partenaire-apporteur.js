import Secondaires from "../secondaire.json"; import Head from "next/head" ; export default function Partenaireapporteur() { let test = Secondaires.map((secondaire) => { return secondaire["data"][10]["contenu"];});return <div><Head> <meta name="description" content="assurance emprunteur le site internet du courtier as du grand lyon vous propose ses services en assurance emprunteur"/> </Head><div className=" pageGenerees pageSecondaire" dangerouslySetInnerHTML={{ __html: test }}></div></div>;}