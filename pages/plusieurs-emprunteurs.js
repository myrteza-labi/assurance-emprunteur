import Articles from "../article.json"; import Head from "next/head" ; export default function Plusieursemprunteurs() { let test = Articles.map((article) => { return article["data"][29]["contenu"];});return <div><Head> <meta name="description" content="Souscrire un credit immobilier a deux necessite que chacun des emprunteurs soit bien assure. Simulation assurance emprunteur."/> </Head><div className=" pageGenerees pageArticle" dangerouslySetInnerHTML={{ __html: test }}></div></div>;}