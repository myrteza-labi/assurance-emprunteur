import Maladies from "../maladies.json"; import Head from "next/head" ; export default function Tension() { let test = Maladies.map((maladie) => { return maladie["data"][33]["contenu"];});return <div><Head> <meta name="description" content="Mettre en concurrence les assurances pour dénicher l\'offre d\'assurance emprunteur avec risque tension dont vous avez besoin pour faire le credit ! "/> </Head><div className=" pageGenerees pagesMaladie" dangerouslySetInnerHTML={{ __html: test }}></div></div>;}