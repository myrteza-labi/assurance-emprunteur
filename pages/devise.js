import Credits from "../credits.json"; import Head from "next/head" ; export default function Devise() { let test = Credits.map((credit) => { return credit["data"][13]["contenu"];});return <div><Head> <meta name="description" content="Réalisez une simulation emprunt en devise à l\'aide du comparateur en ligne, et consultez les offres de plus de 100 organismes financiers"/> </Head><div className=" pageGenerees pageCredit" dangerouslySetInnerHTML={{ __html: test }}></div></div>;}